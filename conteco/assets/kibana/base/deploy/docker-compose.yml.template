---
version: '3'
services:

  ${CONTECO_DC_GLOBAL_SERVICE_NAME}:
    image: ${CONTECO_REGISTRY}${CONTECO_REALM}/${CONTECO_ECOSYSTEM}.${CONTECO_DC_GLOBAL_IMAGE_NAME}
    environment: [${CONTECO_DC_GLOBAL_ENVIRONMENT}] 
    command: [${CONTECO_DC_KIBANA_COMMAND}]
    volumes: [${CONTECO_DC_KIBANA_VOLUMES}]
    ports: [${CONTECO_DC_KIBANA_PORTS}]
    networks: [${CONTECO_DC_KIBANA_NETWORKS}]
    deploy: {${CONTECO_DC_KIBANA_DEPLOY}}

networks:
  ${CONTECO_DC_KIBANA_NETWORKS}:
    external: true

